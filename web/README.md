# StockHub web (Next.js)

Next.js App Router frontend for StockHub. See the [monorepo README](../README.md) for setup, environment variables, and development commands.

```bash
npm install
npm run dev
```

The app rewrites `/api/*` to the Express API (`API_URL` in the root `.env`).
